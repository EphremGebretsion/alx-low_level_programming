#include<stdio.h>
#include "main.h"
/**
 * add - adds the number
 * @a: accepts the first numbet
 * @b: accepts the second number
 * Return: the result
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
