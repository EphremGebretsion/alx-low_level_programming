#include<stdio.h>
/**
 * main - this is used to execute everything
 * the printf will print the coted parts
 * Return: 0 after all that
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
